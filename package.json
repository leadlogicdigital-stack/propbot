{
  "name": "propbot",
  "version": "1.0.0",
  "description": "PropBot - AI Property Valuation Chatbot",
  "main": "api/server.js",
  "scripts": {
    "start": "cd api && npm start",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd api && npm install && cd ../frontend && npm install"
  },
  "dependencies": {},
  "engines": {
    "node": "22.x"
  }
}
